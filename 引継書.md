# KR-clearchecker 引継書

## プロジェクト概要

**正式名称**: かわロボ クリアチェッカー 2026 (KR-clearchecker)  
**企画元**: [下剋上(かわロボ) note記事](https://note.com/jolly_roses919/n/n93ba68eadac8)  
**公開URL**: `https://sin1.studio/KR-clearchecker/`  
**ランキング**: `https://sin1.studio/KR-clearchecker/ranking.html`  
**リポジトリ**: `https://github.com/sin1n24/KR-clearchecker`（masterブランチ）

---

## ファイル構成

```
index.html        # チェッカー本体（単一ファイル）
ranking.html      # ランキングページ
rankings.json     # ランキングデータ（GASが自動更新）
引継書.md          # このファイル
```

### 外部依存（CDN）
- Google Fonts: Outfit / Noto Sans JP
- SheetJS: `https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js`

---

## チェッカー本体（index.html）

### 機能
- 100項目（4カテゴリ×25項目）をカード形式で表示
- カードタップで達成/未達成トグル → LocalStorage（`kr_checker_v4`）に自動保存
- フィルターボタンでカテゴリ絞り込み
- プログレスバーでX/100をリアルタイム表示
- **新規作成**: 確認ダイアログ付きで全データをリセット（`confirmReset()` → `doReset()`）
- **Excel取込**: 配布Excelの「チェッカー」シートから「達成」セルを読み込む（SheetJS）
- **シェア**: X(Twitter)投稿テキスト自動生成 + `?d=` 付きURLを末尾に含める
- **ランキング投稿**: GAS WebApp に達成データを POST（ハンドル名を prompt で入力）

### カテゴリ構成
| カテゴリ | 項目ID | CSS変数 |
|----------|--------|---------|
| 戦績編 | 0〜24 | `--battle: #3b6ef0` |
| 制作編 | 25〜49 | `--make: #c98a00` |
| やらかし編 | 50〜74 | `--fail: #1a9e52` |
| 経験その他編 | 75〜99 | `--exp: #d93f74` |

### URL共有機能（`?d=` パラメータ）

100項目のON/OFFを **100ビット → 13バイト → base64url → 約18文字** に圧縮してURLに付与する。

```
https://sin1.studio/KR-clearchecker/?d=AAEIAQAAAAAgAAAA
```

**`?d=` なし（通常）**: LocalStorageで管理。チェック・Excel取込・新規作成が有効。

**`?d=` あり（シェアリンク）**: URLの達成状況を表示。チェック操作・ランキング投稿は無効。  
data-rowに「📥 このデータで始める」（LocalStorageに保存して通常ページへ）と  
「↩ 自分のページへ」（`?d=`なしのURLへ）が表示される。

シェアボタンが`?d=`付きURLを生成するので、ツイートして後から更新ツイートする用途に使える。

#### encode/decode（JS内）

```javascript
function encodeAchieved(s){          // Set<number> → base64url文字列
  const b=new Uint8Array(13);
  s.forEach(i=>{if(i>=0&&i<100)b[i>>3]|=1<<(i&7);});
  let t='';b.forEach(v=>t+=String.fromCharCode(v));
  return btoa(t).replace(/\+/g,'-').replace(/\//g,'_').replace(/=/g,'');
}
function decodeAchieved(str){         // base64url文字列 → Set<number>
  const pad=(4-str.length%4)%4;
  const t=atob(str.replace(/-/g,'+').replace(/_/g,'/')+('='.repeat(pad)));
  const s=new Set();
  for(let i=0;i<100;i++){if(t.charCodeAt(i>>3)&(1<<(i&7)))s.add(i);}
  return s;
}
```

### エフェクト
- **通常チェック時**: カテゴリ色ベースのクラッカー（`fireCracker(cat)`）
- **マイルストーン（10/25/50/75/100）**: 白フラッシュ→リング→パーティクル爆発（`showMilestone(n)`）

### めくりイラスト（「🖼 全体を見る」ボタン）
- 全画面オーバーレイ、10×10マスのマスクが達成済みブロックから順番に落下して消える
- `const PIXELS` = 2500要素の配列（50×50グリッド、`'w'`=白、`'#RRGGBB'`=色付き）

#### ブロック座標の対応
```
戦績(0-24):       [row=id//5,        col=id%5      ]
制作(25-49):      [row=(id-25)//5,   col=5+(id-25)%5]
やらかし(50-74):  [row=5+(id-50)//5, col=(id-50)%5  ]
経験(75-99):      [row=5+(id-75)//5, col=5+(id-75)%5]
```

#### Excelからイラストを再抽出する方法（変更時）
```python
import openpyxl, json

wb = openpyxl.load_workbook('かわロボクリアチェッカー2026.xlsx')
ws = wb['めくりイラスト']

def is_near_white(rgb):
    r,g,b = int(rgb[2:4],16), int(rgb[4:6],16), int(rgb[6:8],16)
    return r>240 and g>240 and b>240

flat = []
for r in range(2, 52):
    for c in range(1, 51):
        cell = ws.cell(row=r, column=c)
        rgb = cell.fill.fgColor.rgb if cell.fill and cell.fill.fgColor else 'FFFFFFFF'
        if not rgb or rgb == '00000000': rgb = 'FFFFFFFF'
        flat.append('w' if is_near_white(rgb) else '#'+rgb[2:])

print(json.dumps(flat, separators=(',',':')))
# → index.html の const PIXELS= に貼り付け
```

---

## ランキング（ranking.html + rankings.json + GAS）

### 仕組み

```
index.html ──POST──→ GAS WebApp ──Git API──→ rankings.json（リポジトリ）
                                                     ↑
ranking.html ──fetch──────────────────────────────────┘
```

### GAS WebApp
- **スクリプトID**: `1_-340gn7yFl8sau31FWgtLGd4K2KObRk42QVTWRnCX9QBC_ZXCNtZpGV`
- **デプロイID**: `AKfycbwCuKjzz8q03CvQ8fiMR7Qlj83LI_kz2ta4K5eBUvmKJloBqN8RPmn9SgSxNU5Wocwy`
- **エンドポイント**: `https://script.google.com/macros/s/AKfycbwCuKjzz8q03CvQ8fiMR7Qlj83LI_kz2ta4K5eBUvmKJloBqN8RPmn9SgSxNU5Wocwy/exec`
- **ローカルソース**: `C:\Users\sin1n\Downloads\KR-clearchecker-GAS\code.gs`
- **アクセス設定**: 次のユーザーとして実行=自分、アクセス=全員
- **ScriptProperties**: `GITHUB_TOKEN`（sin1n24の GitHub PAT、MiniKawaRoboGASと同一トークン）

#### GAS再デプロイ手順（変更時）
```powershell
cd C:\Users\sin1n\Downloads\KR-clearchecker-GAS
clasp push
clasp deploy --deploymentId AKfycbwCuKjzz8q03CvQ8fiMR7Qlj83LI_kz2ta4K5eBUvmKJloBqN8RPmn9SgSxNU5Wocwy
```
> **注意**: スクリプトを初めて承認する場合はGASエディタで `checkDeployment()` を手動実行して承認を完了させる。

### rankings.json 形式
```json
{
  "updated": "2026-06-20T12:00:00.000Z",
  "entries": [
    {
      "handle": "ハンドル名",
      "submitted": "2026-06-20T11:00:00.000Z",
      "total": 42,
      "battle": 12,
      "make": 15,
      "fail": 8,
      "exp": 7,
      "achieved": [0, 3, 7, 12, 25, 26]
    }
  ]
}
```
- `handle` が同じ投稿は上書き（後勝ち）
- GASが GitHub Contents API でコミットするため自動でリポジトリに反映される

### ranking.html の3タブ構成
| タブ | 内容 |
|------|------|
| 🏆 総合 | 達成数降順。積み上げカラーバーでカテゴリ内訳を可視化 |
| 👑 カテゴリ王 | 各カテゴリ最多達成者をconic-gradientリングで表示 |
| 📊 項目別達成率 | 100項目を達成率降順で表示、X/N人を併記 |

---

## デプロイ・更新手順

### index.html / ranking.html を更新する場合
```powershell
# ソースを編集後
cd C:\Users\sin1n\Downloads\KR-clearchecker-repo
git add index.html ranking.html
git commit -m "変更内容"
git push origin master
# GitHub Pages（sin1.studio カスタムドメイン）に自動反映（数分）
```

> **文字化けに注意**: PowerShell で日本語ファイルを読み書きするときは必ず
> `[System.IO.File]::ReadAllText(path, [System.Text.Encoding]::UTF8)` /
> `[System.IO.File]::WriteAllText(path, content, New-Object System.Text.UTF8Encoding($false))`
> を使うこと。`Get-Content`/`Set-Content` はShift-JIS/BOM混入で文字化けする。

### カスタムドメイン設定
- リポジトリに `CNAME` ファイル（内容: `sin1.studio`）が必要
- GitHub Pages Settings でカスタムドメインを `sin1.studio` に設定済み

---

## 開発経緯メモ
- Claude Code（claude.ai/code）との対話で設計・実装
- 元ExcelのチェッカーシートをWeb化。既存Excel入力済みユーザーの引継ぎも考慮
- イラストシートの50×50セル色データをPython(openpyxl)で抽出してJS埋め込み
- デザイン: ライトベージュテーマ、Outfit + Noto Sans JP、PC 5列 / スマホ 2列レスポンシブ
