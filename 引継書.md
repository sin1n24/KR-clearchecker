# KR-clearchecker 引継書

## プロジェクト概要

**正式名称**: かわロボ クリアチェッカー 2026 (KR-clearchecker)  
**企画元**: [下剋上(かわロボ) note記事](https://note.com/jolly_roses919/n/n93ba68eadac8)  
**形式**: 単一HTMLファイル（GitHub Pagesで公開予定）  
**現在の成果物**: `KR-clearchecker.html`

---

## 機能一覧

### チェッカー本体
- 100項目（4カテゴリ×25項目）をカード形式で表示
- カードタップで達成/未達成トグル
- LocalStorage (`kr_checker_v4`) に自動保存
- フィルターボタンでカテゴリ絞り込み

### カテゴリ構成
| カテゴリ | 項目ID | 色 |
|----------|--------|-----|
| 戦績編 | 0〜24 | `#4a7cf5` |
| 制作編 | 25〜49 | `#f5c842` |
| やらかし編 | 50〜74 | `#5ecb6e` |
| 経験その他編 | 75〜99 | `#e8609a` |

### エフェクト
- **通常チェック時**: 画面左右の端からカテゴリ色ベースのクラッカーが飛び出す（`fireCracker(cat)` / `spawnCrackerSide()`）
- **マイルストーン（10/25/50/75/100）**: 白フラッシュ→衝撃波リング→80パーティクル爆発→クラッカー連射のド派手演出（`showMilestone(n)`）

### めくりイラスト（「🖼 全体を見る」ボタン）
- 全画面オーバーレイで表示
- 100個のマス（10×10）がすべて黒くマスクされた状態で開く
- 達成済みマスが左上→右下のスキャン順に1枚ずつ落下アニメーション（`blockDrop`）で消え、絵が現れる
- 未達成マスは黒いまま残る
- アニメーション間隔: `min(180ms, 3500ms/達成数)` 単位で順次起動

#### イラストデータ構造
- Excelの「めくりイラスト」シートから抽出したピクセルデータを埋め込み済み
- `const PIXELS` = 2500要素の配列（50×50グリッド、`'w'`=白、`'#RRGGBB'`=色付き）
- 10×10ブロック構造（各ブロック=5×5ピクセル）
- item_id → ブロック座標の変換: `itemToBlock(id)` 関数

```
戦績(0-24):    block_row = id//5,       block_col = id%5
制作(25-49):   block_row = (id-25)//5,  block_col = 5 + (id-25)%5
やらかし(50-74): block_row = 5+(id-50)//5, block_col = (id-50)%5
経験(75-99):   block_row = 5+(id-75)//5, block_col = 5+(id-75)%5
```

### データ管理（ツールバー直下の常設バー）
- **JSON書き出し**: 達成状況をJSONファイルとしてダウンロード（デバイス間引継用）
- **JSON読込**: 書き出したJSONを読み込んで復元
- **Excel取込**: 配布された `かわロボクリアチェッカー2026.xlsx` の「チェッカー」シートから「達成」セルを読み込む（SheetJS使用）
- **リセット**: 確認ダイアログ付き（`confirmReset()` → `doReset()`）

### JSON形式
```json
{
  "version": 2,
  "exported": "2026-06-20T00:00:00.000Z",
  "achieved": [0, 3, 7, 25, ...],
  "total": 4
}
```

### シェア
- X(Twitter)投稿テキスト自動生成
- カテゴリ別達成数を含むテキストをクリップボードコピーまたは直接ポスト

---

## ファイル構成

```
KR-clearchecker.html   # 単一ファイル（外部依存はCDNのみ）
```

### 外部依存
- Google Fonts: Noto Sans JP
- SheetJS (xlsx): `https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js`

---

## GitHub Pages公開手順

1. リポジトリ名を `KR-clearchecker` として作成
2. `KR-clearchecker.html` を `index.html` にリネームしてpush
3. Settings → Pages → Branch: main / (root) で公開
4. 公開URL: `https://sin1n24.github.io/KR-clearchecker/`

---

## 未実装・今後の拡張予定

### その他TODO
- [ ] イラスト再描画（Excelから差し替えがあった場合はPythonスクリプトで再抽出）
- [ ] OGP/SNSカード設定（GitHub Pages公開後）
- [ ] スマホでのタッチ操作最適化確認

---

## ランキング・多人数比較機能 設計案

### 概要

複数参加者の達成状況を集計・比較するための仕組み。  
ミニかわロボ登録システムと同構成のため、既存GASコードを流用できる。

### システム構成

```
参加者のブラウザ                  GitHub リポジトリ
┌─────────────────────┐           ┌──────────────────────────┐
│  index.html          │           │  rankings.json           │
│                     │  POST     │  (参加者データ一覧)       │
│ 「結果を投稿」ボタン  │──────────→│                          │
│  ↓ 名前入力ダイアログ │           │  ↑ GAS WebApp が         │
│  ↓ achieved配列送信  │           │    Git Data API で       │
└─────────────────────┘           │    コミット              │
           ↓                      │                          │
   Google Apps Script             │  ranking.html            │
   (Webhook として動作)            │  (rankings.jsonをfetch   │
                                  │   して表示)              │
                                  └──────────────────────────┘
```

### データ設計

#### rankings.json（GitHubリポジトリに保存）

```json
{
  "updated": "2026-06-20T12:00:00Z",
  "entries": [
    {
      "handle": "Leopard_AI",
      "submitted": "2026-06-20T11:00:00Z",
      "total": 42,
      "battle": 12,
      "make": 15,
      "fail": 8,
      "exp": 7,
      "achieved": [0, 3, 7, 12, 25, 26]
    }
  ]
}
```

- `handle` をキーとして同一人物の再投稿は上書き（後勝ち）
- `achieved` 配列があると項目別達成率の計算が可能
- 投稿は任意。プライバシー配慮で `achieved` なしの総数のみ送信オプションも検討

#### index.html から送信するペイロード

```json
{
  "handle": "Leopard_AI",
  "total": 42,
  "battle": 12,
  "make": 15,
  "fail": 8,
  "exp": 7,
  "achieved": [0, 3, 7, 12, 25, 26]
}
```

### GAS WebApp コード骨格

ミニかわロボ登録システムの `doPost()` をベースに改造する。

```javascript
const GITHUB_TOKEN = PropertiesService.getScriptProperties().getProperty('GITHUB_TOKEN');
const REPO_OWNER   = 'sin1n24';
const REPO_NAME    = 'KR-clearchecker';
const FILE_PATH    = 'rankings.json';
const BRANCH       = 'main';

function doPost(e) {
  const payload = JSON.parse(e.postData.contents);

  // 現在の rankings.json を取得
  const getRes = UrlFetchApp.fetch(
    `https://api.github.com/repos/${REPO_OWNER}/${REPO_NAME}/contents/${FILE_PATH}`,
    { headers: { Authorization: `token ${GITHUB_TOKEN}` } }
  );
  const file    = JSON.parse(getRes.getContentText());
  const sha     = file.sha;
  const current = JSON.parse(Utilities.newBlob(
    Utilities.base64Decode(file.content.replace(/\n/g, ''))
  ).getDataAsString());

  // 同一ハンドルは上書き、新規は追加
  const idx   = current.entries.findIndex(en => en.handle === payload.handle);
  const entry = { ...payload, submitted: new Date().toISOString() };
  if (idx >= 0) current.entries[idx] = entry;
  else          current.entries.push(entry);
  current.updated = new Date().toISOString();

  // GitHub にコミット
  UrlFetchApp.fetch(
    `https://api.github.com/repos/${REPO_OWNER}/${REPO_NAME}/contents/${FILE_PATH}`,
    {
      method: 'PUT',
      headers: {
        Authorization: `token ${GITHUB_TOKEN}`,
        'Content-Type': 'application/json'
      },
      payload: JSON.stringify({
        message: `update ranking: ${payload.handle}`,
        content: Utilities.base64Encode(
          JSON.stringify(current, null, 2),
          Utilities.Charset.UTF_8
        ),
        sha,
        branch: BRANCH
      })
    }
  );

  return ContentService
    .createTextOutput(JSON.stringify({ ok: true }))
    .setMimeType(ContentService.MimeType.JSON);
}
```

**セットアップ手順**
1. GAS プロジェクトを新規作成
2. ScriptProperties に `GITHUB_TOKEN`（repo スコープのPAT）を登録
3. 「ウェブアプリとしてデプロイ」→ 実行者: 自分、アクセス権限: 全員
4. 発行されたURLを `index.html` の `fetch()` 先に設定

### index.html への追加実装（投稿ボタン）

ツールバーに追加するボタンと送信関数：

```javascript
function submitRanking() {
  const handle = prompt('ハンドル名を入力してください（X/SNSアカウント等）');
  if (!handle || !handle.trim()) return;

  const cats = {};
  ['battle','make','fail','exp'].forEach(c =>
    cats[c] = ITEMS.filter(i => i.cat === c && achieved.has(i.id)).length
  );

  fetch('https://script.google.com/macros/s/YOUR_DEPLOY_ID/exec', {
    method: 'POST',
    body: JSON.stringify({
      handle: handle.trim(),
      total: achieved.size,
      ...cats,
      achieved: [...achieved]
    })
  })
  .then(() => showToast('📊 投稿しました！'))
  .catch(() => showToast('投稿に失敗しました'));
}
```

```html
<!-- ツールバーに追加 -->
<button class="tb" onclick="submitRanking()">📊 結果を投稿</button>
```

### ranking.html の表示ビュー

| ビュー | 内容 |
|--------|------|
| 総合ランキング | 達成数降順。ハンドル・総数・カテゴリ別内訳バー |
| カテゴリ王 | 戦績王 / 制作王 / やらかし王 / 経験王を各1名表彰 |
| 項目別達成率 | 100項目それぞれが何%の参加者に達成されたか（棒グラフ） |
| コミュニティイラスト | 全員の達成マスを重ねたヒートマップをめくりイラストに重ねて表示 |

### セキュリティ・運用上の注意

- GAS WebApp は公開URLのため、なりすましは技術的に防げない
- 対策として投稿時に企画者配布のパスフレーズ照合を追加することも可能
- ハンドル名は自己申告・後勝ち上書きのため、年末集計前に企画者が目視確認を推奨
- **GitHub Token は GAS の ScriptProperties に保存し、コードに直書きしない**

### 実装順序

1. `rankings.json`（空の初期ファイル）をリポジトリに追加
2. GAS WebApp を作成・デプロイ → URLを取得
3. `index.html` に「📊 結果を投稿」ボタンと送信関数を追加、GAS URLを設定
4. `ranking.html` を作成してリポジトリに追加
5. 自分のデータでテスト投稿して動作確認


---

## Excelからイラストを再抽出する方法

```python
import openpyxl, json

wb = openpyxl.load_workbook('かわロボクリアチェッカー2026.xlsx')
ws = wb['めくりイラスト']

def is_near_white(rgb):
    r,g,b = int(rgb[2:4],16), int(rgb[4:6],16), int(rgb[6:8],16)
    return r>240 and g>240 and b>240

flat = []
for r in range(2, 52):       # row 2-51 = 50行
    for c in range(1, 51):   # col 1-50 = 50列
        cell = ws.cell(row=r, column=c)
        rgb = cell.fill.fgColor.rgb if cell.fill and cell.fill.fgColor else 'FFFFFFFF'
        if not rgb or rgb == '00000000': rgb = 'FFFFFFFF'
        flat.append('w' if is_near_white(rgb) else '#'+rgb[2:])

print(json.dumps(flat, separators=(',',':')))
# → HTMLの const PIXELS= に貼り付け
```

---

## 開発経緯メモ

- Claude（claude.ai）との対話で設計・実装
- 元ExcelのチェッカーシートをWeb化、既存Excel入力済みユーザーの引継ぎも考慮
- イラストシートの50×50セル色データをPython(openpyxl)で抽出してJS埋め込み
- デザイン方針: ダークテーマ、カテゴリ色グロー、モバイルファースト
